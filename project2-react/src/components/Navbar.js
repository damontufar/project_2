import logo from '../logochido.png'

const Navbar = () => {
    return (
        <nav className="navbar navbar-expand-lg navbar-light bg-light fixed-top">
            <img src={logo} alt="Logo chido" width='100' />
        </nav>
    )
}

export default Navbar
